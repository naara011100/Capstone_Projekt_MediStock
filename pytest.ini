[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: unit tests — no database required
    integration: integration tests — requires medistock_test PostgreSQL database
    e2e: end-to-end workflow tests — requires medistock_test PostgreSQL database
