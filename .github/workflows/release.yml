name: Release

# Triggers on version tags such as v1.0.0, v2.3.1, etc.
on:
  push:
    tags:
      - "v*"

jobs:
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write   # needed to push to GitHub Container Registry

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            # e.g. v1.2.3 → ghcr.io/…/medistock:1.2.3
            type=semver,pattern={{version}}
            # e.g. v1.2.3 → ghcr.io/…/medistock:1.2
            type=semver,pattern={{major}}.{{minor}}
            # e.g. v1.2.3 → ghcr.io/…/medistock:1
            type=semver,pattern={{major}}
            # ghcr.io/…/medistock:latest  (every version tag)
            type=raw,value=latest
            # ghcr.io/…/medistock:sha-<short-sha>  (traceability)
            type=sha,prefix=sha-

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
